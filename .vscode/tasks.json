{
    "version": "2.0.0",
    "tasks": [
        // This task is for compiling the Erlang project with Rebar3 as a preLaunchTask for Launch Erlang with Dependencies configuration in launch.json.
        {
            "label": "Rebar3 Compile",
            "type": "shell",
            "command": "rebar3 compile",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$erlang"
        },
    ]
}